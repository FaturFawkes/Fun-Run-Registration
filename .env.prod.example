# PRODUCTION ENVIRONMENT CONFIGURATION
# Copy this file to .env.prod and fill in actual values
# NEVER commit .env.prod to version control!

# ========================================
# DATABASE
# ========================================
DB_USER=tautaurun
DB_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD
DB_NAME=tau_tau_run_prod
DB_SSL_MODE=require
DB_MAX_CONNECTIONS=20
DB_MAX_IDLE_CONNECTIONS=10

# ========================================
# JWT AUTHENTICATION
# ========================================
# Generate with: openssl rand -base64 48
JWT_SECRET=CHANGE_THIS_TO_64_PLUS_RANDOM_CHARACTERS_GENERATED_WITH_OPENSSL
JWT_EXPIRATION_HOURS=24

# ========================================
# SMTP CONFIGURATION
# ========================================
# Production: Use SendGrid, AWS SES, or similar
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USERNAME=apikey
SMTP_PASSWORD=YOUR_SENDGRID_API_KEY_HERE
SMTP_FROM_EMAIL=noreply@tautaurun.com
SMTP_FROM_NAME=Tau-Tau Run Team

# ========================================
# EVENT DETAILS
# ========================================
EVENT_NAME=Tau-Tau Run Fun Run 5K
EVENT_DATE=2026-02-15
EVENT_LOCATION=Gelora Bung Karno Stadium, Jakarta

# ========================================
# CORS & API
# ========================================
CORS_ALLOWED_ORIGINS=https://tautaurun.com,https://www.tautaurun.com
NEXT_PUBLIC_API_URL=https://api.tautaurun.com/api/v1
NEXT_PUBLIC_EVENT_DATE=February 15, 2026

# ========================================
# IMPORTANT SECURITY NOTES
# ========================================
# 1. Change ALL default passwords
# 2. Generate new JWT secret (64+ characters)
# 3. Use strong database password (20+ characters)
# 4. Never commit this file to version control
# 5. Use environment variable management service in production
# 6. Enable SSL/TLS for database connections
# 7. Rotate secrets regularly
